name: nodo_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  mqtt_client: ^10.3.0
  record: ^6.0.0
  permission_handler: ^11.3.1
  provider: ^6.1.2
  uuid: ^4.5.1
  path_provider: ^2.1.4
  path: ^1.9.0
  typed_data: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
